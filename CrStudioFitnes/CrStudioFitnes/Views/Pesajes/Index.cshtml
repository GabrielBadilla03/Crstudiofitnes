@model IEnumerable<CrStudioFitnes.Models.Pesaje>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Peso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Historial)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Peso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Historial.IdUsuario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPesaje">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPesaje">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPesaje">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
